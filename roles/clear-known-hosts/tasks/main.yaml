- name: Clear all cluster hosts from known_hosts file
  loop: "{{ groups['all'] }}"
  ansible.builtin.command: "ssh-keygen -f $HOME/.ssh/known_hosts -R {{ hostvars[item].ansible_hostname }}"
  changed_when: true
