host: ""

smtp:
  host: ""
  port: ""
  user: ""
  password_secret:
    namespace: kube-system
    name: mail-smtp
    key: password
  from_address: ""

ingress: true

storage:
  size: 10Gi
  encrypted: false
  auto_backup: true

zone: "" # Specific zone to localize this release to (pods and volumes)

snapshots:
  enabled: true
  class_name: snap
