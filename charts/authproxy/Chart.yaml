apiVersion: v2
name: authproxy
description: Small oauth2-proxy deployment, which can be used to auth all subdomains of a domain
type: application
version: "0.0.1"
appVersion: "0.0.1"

dependencies:
  - name: common
    version: "^1"
    repository: https://charts.bitnami.com/bitnami
