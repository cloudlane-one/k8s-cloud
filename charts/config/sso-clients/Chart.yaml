apiVersion: v2
name: sso-clients
description: Deploy OIDC or SAML clients via keycloak-config-cli
type: application
version: "0.1.2"
appVersion: "0.1.2"

dependencies:
  - name: common
    version: "^1"
    repository: https://charts.bitnami.com/bitnami
