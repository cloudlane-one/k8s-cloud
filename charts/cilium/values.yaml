host: ""

oauth2ProxyHost: ""

admin_group: ""

clusterIssuer: "acme"

externalDNSManager: "cluster"

bootstrap: false # Set to true to install in bootstrap mode (minimum deployment to support core functions and CRs)

# Bootstrap values
cilium:
  encryption:
    # See https://docs.cilium.io/en/stable/gettingstarted/encryption-wireguard/
    enabled: true
    type: wireguard
    wireguard:
      userspaceFallback: true
  l7Proxy: false
